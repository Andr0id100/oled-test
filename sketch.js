function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0)
}

function draw() {
  
}

function mouseClicked() {
  stroke(255, 0, 0)
  strokeWeight(5)
  fill(0, 0, 255) 
  
    circle(mouseX, mouseY, 20)

}